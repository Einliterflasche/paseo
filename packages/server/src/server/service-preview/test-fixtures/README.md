These self-signed certificate/key files are local wire-test assets for `control.test` and `127.0.0.1`. They are supplied only to the test HTTPS server and its client trust option. They confer no production trust and contain no live service credential. The certificate expires in September 2036; regenerate it for the same test names when needed. Tests keep normal certificate verification enabled.
